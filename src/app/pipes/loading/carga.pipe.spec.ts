/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CargaPipe } from './carga.pipe';

describe('Pipe: Cargae', () => {
  it('create an instance', () => {
    let pipe = new CargaPipe();
    expect(pipe).toBeTruthy();
  });
});
